# Functional Requirements
- Browse products (low latency via CDN + Redis)
- Shopping cart persistence across devices (no data loss)
- Checkout via third-party payment gateway (no PAN stored)
- Event-driven order processing (Service Bus)
- Product search (Azure Cognitive Search)
- Product images in Blob Storage + CDN
